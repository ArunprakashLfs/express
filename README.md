### express learning

provide a movie api for 
reactjs app

### importantcommands

`npm install`
`npm run server`
`git init`
`git add`
`git commit -m "your message"`
`git push -u origin main` or `git push`